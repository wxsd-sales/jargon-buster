export const SANDBOX_ADMIN_EMAIL_REGEX = /^admin@.+\.wbx\.ai$/;

export const SDK_CONFIG = {
  logger: { level: 'silent' },
  meetings: { reconnection: { enabled: true } }
};

export const EAF_CONFIG = { logs: { logLevel: 2 } };
